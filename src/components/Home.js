import React from 'react';
import DocForm from '../components/DocForm';
import './css/Home.css';

const Home = ()=> (
    <section className="centered">
        <DocForm />
    </section>
    // <section className="centered"><p>This is the <strong>Home</strong> route.</p></section>
);

export default Home;